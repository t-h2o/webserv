#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>

namespace utils
{

std::string my_replace(std::string str, std::string find, std::string replace);
void		check_path_and_method();

} /* namespace utils */

#endif /* UTILS_HPP */
